# Orbitra Launcher installer

1. Build the Windows release package with `python publish.py windows --x64-only`.
2. Compile `SS14.Launcher.iss` with Inno Setup 6.7 or newer.
3. The installer is written to `bin/installer/Orbitra.Launcher.Setup.x64.exe`.
